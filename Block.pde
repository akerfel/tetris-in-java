public class Block {
    boolean isFilled;
    boolean isStuck;
  
    public Block(boolean isFilled, boolean isStuck) {
        this.isFilled = isFilled;
        this.isStuck = isStuck;
    }
}
